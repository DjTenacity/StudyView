package com.gdj.myview.utils;

import java.io.File;

/**
 * 作者：${LoveDjForever} on 2017/7/17 17:55
 *  * 邮箱： @qq.com
 */

public class FileStudyUtils {
    /**有四个构造函数
     * 1.File(File dir,String name)
     * 参数为File和String，File制定构造的新的File对象的路径，而String制定新的File名字
     * 2.File(String path)
     * 参数为String，构造一个新的File使用制定的路径
     * 3.File(String dirPath,String name)
     * dirPath为指定的文件路径，name为文件的名字
     * 4.File(URI uri)
     *  构造新的文件使用URI指定路径
     *
     * File getFilesDir();返回路径中的目录，并且保持应用程序文件。
     * 在这里获取到的是/data/data//files目录，返回一个File
     *
     *
     * **/


}
